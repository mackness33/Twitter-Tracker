<html>
 <head>
   <title>Twitter Tracker</title>
   <meta charset="utf-8">
   <style type="text/css">
      p {
         margin-top: 1.5vw;   
      }
      h1 {
         text-align: center;
         margin-top: 3vw;
         margin-bottom: 3vw;
         color: #50ABF1;
         font-size: 5vw;
         font-family: Helvetica;
      }
      #input, #button {
         height: 3vw;
         width: 10vw;
         text-align: center;
         font-size: large;
         border-radius: 5px;
         border-color: rgb(0, 174, 255);
         font-family: Arial, Helvetica, serif;
      }
      #input {
         margin-top: 1.15vw;
         margin-left: 38vw;
         margin-right: 3vw;
         float: left;
      }
      #button {
         font-weight: bold;
         background-color: blue;
         color: orange;
      }
      #descrizione {
         font-size: 0.75vw;
         text-align: center;
      }
   </style> 

   <script type="text/javascript">

      /*function richiestaP(stringa)
      {
         return jasone;
      };

      function stampone(jason)
      {
         d = document.getElementById("gionni");
         for(x in jason)
         {
            d.appendChild(x);
         }
      };    

      var clicca = function() {
         var ricerca = document.getElementById("argomento_da_cercare").value;
         svolgiRicerca(ricerca);
      };
      function svolgiRicerca(stringa){
         if (stringa[0] == '#' || stringa[0] == "$" || stringa[0] == "@") {
            v = richiestaP(stringa);
            stampone(v);
         }
         else {
            alert("input non valido");
         };
      };


      /*function ricercaParolaChiave(stringa){
         richiestaP()
         alert("imp");
      };
      function ricercaLuogo(stringa){
         alert("er");
      };
      function ricercaPersona(stringa){
         alert("lo stalking è un reato penale");
      };*/
      
 	</script> 
 </head>
 <body>
 <h1>Twitter Tracker</h1>
 <form action="/base" method="post" target="_self">
      <input id="input" type="text" name="ricerca" ><br>
      <input id="button" type="submit" value="Search" />
 </form>
 <p id="descrizione">
    Per cercare: <br>
    -una parola chiave scrivi la parola preceduta dal carattere #, <br>
    -una zona scrivi il nome della zona preceduta dal carattere $, <br>
    -gli spostamenti di una persona scrivi il nome utente di quella persona (compresa la @). <br>
 </p> 
 <p id="gionni">
 </p>
 </body>
</html>
